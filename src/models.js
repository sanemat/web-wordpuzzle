// @ts-check
/**
 * @typedef {{
 *   type: string,
 *   playerId: number,
 * }} Pass
 */
/**
 * @typedef {{
 *   type: string,
 *   playerId: number,
 * }} Resign
 */
/**
 * @typedef {{
 *   type: string,
 *   playerId: number,
 *   panels: Panel[],
 * }} Swap
 */
/**
 * @typedef {{
 *   width: number,
 *   height: number,
 * }} BoardMeta
 */
/**
 * @typedef {string} Panel the panel.
 * @typedef {Panel|null} BoardPanel
 */

export function _dummy() {
  return true;
}
